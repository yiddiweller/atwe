# Generated by build.js — the masks that matter are embedded in public/index.html
# as the --nv-*-off / --nv-*-on custom properties. Keeping a second copy here would
# just be one more thing that can drift out of step with the app.
built.json
*-off.png
*-on.png
