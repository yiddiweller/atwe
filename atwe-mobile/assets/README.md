# Assets

Replace these placeholders with real artwork before shipping:

- `icon.png` — 1024×1024, the Atwe app icon (no transparency for iOS).
- `adaptive-icon.png` — 1024×1024 foreground for Android adaptive icon.
- `splash.png` — the Atwe mark centered on `#07080A` (used by the splash plugin).

The placeholders are solid Atwe-black squares so the project builds; they are not
final art. Recommended: export from the same source as the web `logo-mark.png`.
