import { WorldPlaceholder } from '@/components/WorldPlaceholder';

// Engine — discovery & commerce (search, marketplace, jobs, services, businesses).
// Phase 4 wires /api/search + the marketplace and jobs surfaces.
export default function Engine() {
  return (
    <WorldPlaceholder
      title="Engine"
      subtitle="Search and discover the world — marketplace, jobs, services and businesses."
      icon="search"
      phase="Phase 4 · Discovery"
    />
  );
}
